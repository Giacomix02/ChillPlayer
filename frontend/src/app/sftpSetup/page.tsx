import s from './page.module.css';

export default function SftpSetupPage() {
    return (
        <div className={s.container}>
            <h1>Sftp setup page</h1>
            <p>This is the Sftp setup page.</p>
        </div>
    );
}